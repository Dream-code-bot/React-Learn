import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
        <div>
            Let's start Learning Components
        </div>
    );
}

ReactDOM.render(<App />, document.querySelector('#root'));